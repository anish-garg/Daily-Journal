<%- include("header") -%>
    <% newBlog.forEach(function(item){ %>
        <h1>
            <%= item.title %>
        </h1>
        <p>
            <%= item.content %>
        </p>
        <% }) %>
            </div>
            </body>

            </html>